import { FilterByNamePipe } from './filter-by-name.pipe';

describe('FilterByNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
